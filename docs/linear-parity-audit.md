# Linear parity audit

Baseline: 2026-09-15. Linear behavior is compared against the official documentation listed in the task. A feature is not marked as passing when its screen is backed by mock data or a placeholder.

Reference set: [Linear Docs](https://linear.app/docs), [Projects](https://linear.app/docs/projects), [Project templates](https://linear.app/docs/project-templates), [Labels](https://linear.app/docs/labels), [Teams](https://linear.app/docs/teams), [Issue properties](https://linear.app/docs/issue-properties), [Cycles](https://linear.app/docs/cycles), [Views](https://linear.app/docs/views), [Issue relations](https://linear.app/docs/issue-relations), [Notifications](https://linear.app/docs/notifications), and [Initiative and Project updates](https://linear.app/docs/initiative-and-project-updates).

| Linear feature                                     | Linear behavior                                                                                                                               | Circle current state                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Missing/incorrect                                                                                                                       | Planned fix                                                                                                           | Test evidence                                                                                                                                                          |
| -------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Workspace and teams                                | Workspace/team membership scopes configuration and work                                                                                       | Real workspace/team APIs exist; workspace, team, member, team-member listing/join/add/update/delete paths now require authenticated workspace/team visibility; team creation and workspace routes require an explicit real workspace context                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Some legacy UI imports mock types/constants; multi-workspace settings need broader audit                                                | Replace remaining mock-backed screens and verify workspace isolation                                                  | Access-control predicate tests; workspace-selection tests; backend typecheck; live Google-login/onboarding flow                                                        |
| Issues                                             | CRUD, properties, relations, hierarchy, activity, filters                                                                                     | CRUD/activity APIs plus persisted issue templates; relation add/remove now persists IDs/types and same-team candidate selection; create/update validates status, priority, estimate, assignee, project, cycle and parent team/workspace invariants; team-configured estimate scales persist on real teams and issue estimate changes record activity; comment mentions/notification recipients are scoped; issue detail subscription is wired to the persisted API and saved-view issue filters are applied to live results; reactions can now be toggled off by the authenticated member with team access checks; all-issues, search, cycle, project, My Issues, and member-profile lists and filter option catalogs now render from scoped API query results only                                                              | Some list/filter screens still use mock types; bulk-action semantics and estimate-filter UI need more integration coverage       | Keep API response as the only record source and add the remaining issue contract tests                                | Issue-property, estimate-scale, advanced-estimate-filter and reaction-scope tests; issue-template unit tests; access/scope regression checks; local/CI build; production API/UI smoke |
| Projects                                           | Overview, properties, milestones, updates, labels, issues, members, activity, progress and subscriptions                                      | Core project APIs, workspace-scoped member replacement/listing, persisted project activity and template instantiation exist; projects support persisted multi-team membership and issue reads preserve projects on secondary teams; missing lead data stays null rather than fabricated; project subscriptions are persisted per authenticated member and exposed through scope-checked API/UI controls; project detail and peek surfaces now edit persisted members through a workspace-scoped picker; project updates can be edited/deleted only by their author, with latest-health rollback after deletion                                                                                                                                                                                                               | Some project detail surfaces still use mock types; project-level multi-team integration coverage remains                                | Add remaining project views and contract/E2E coverage for team membership and cross-team issue tabs                   | Project activity/member/subscription/update API tests; member-isolation tests; project-template create/edit/duplicate/live E2E                                                |
| Project templates                                  | Workspace/team templates can configure projects, milestones and issues; templates can be edited, duplicated, deleted and used during creation | Real entity, migration, CRUD/duplicate/delete API, settings UI and create flow; template project snapshots can carry validated teamIds into multi-team instantiation; cloned issue milestone references are remapped to the newly created milestone name rather than leaking the source/new UUID                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | A real-database failure-path integration run remains desirable beyond the transactional unit harness                                    | Add rollback/remapping integration suite; keep invalid references atomic                                              | Parent/relation/rollback/milestone remap regression tests; CI/deploy plus live production instantiation with overrides |
| Issue templates                                    | Workspace/team defaults can prefill issue properties and rich content                                                                         | Real entity, migration, CRUD/duplicate/delete API, settings UI and new-issue selector; project, cycle, due date, all live workflow statuses/priorities, labels, structured Markdown blocks, parent issue and milestone defaults persist through the API and are applied to created issues; backend rejects cross-team project/cycle/parent combinations, rejects a configured milestone outside the selected project, and revalidates duplicates; the settings editor searches live same-team issues for parent selection and live project milestones for milestone selection, with query errors surfaced                                                                                                                                                                                                                                                                                                                                   | Broader integration coverage remains; legacy templates without a project can still carry the existing free-text milestone value          | Add contract/E2E coverage incrementally and migrate legacy free-text milestones where product policy permits             | Normalization/parent/milestone-scope tests; full project-service regression (45/45 suites, 133/133 tests); web lint/typecheck/build; migration/schema checks |
| Project and issue labels                           | Reusable workspace/team labels with issue/project scope, descriptions, groups and optional mutually-exclusive behavior                        | Real persisted labels support optional team ownership; label list APIs return workspace-global plus active-team labels; settings UI can filter, create, reassign and clear team ownership for issue/project labels; issue templates, issue mutations, project mutations, and label creation/update validate team/workspace ownership before persistence; group/scope and mutually-exclusive rules remain backend-enforced                                                                                                                                                                                                                                                                                            | Team-level integration/E2E coverage remains; label colors remain a simple CSS-color field                                               | Add contract/E2E coverage for team label assignment, scope isolation and mutually-exclusive enforcement               | Label-scope service tests; migration/typecheck/lint/build; production API/UI smoke                                                                                     |
| Initiatives                                        | Group projects and show progress                                                                                                              | Workspace-scoped CRUD/delete/linking, persisted activity/progress, persisted resources and labels, initiative health updates with author/blocks, live create/settings/detail flows, an editor for persisted properties including clearing optional values, and author-scoped update edit/delete with health rollback when the latest update is removed                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Broader integration coverage remains; update reactions, reminders and external delivery remain incomplete                              | Add initiative contract/E2E coverage; add update reactions/reminders when provider contracts exist                   | Migration20260915230000; initiative update author/isolation tests; backend/frontend build                                                                                                           |
| Cycles                                             | Team-scoped cycles with dates, issues and progress; optional cadence settings can keep upcoming cycles scheduled                              | Team-scoped CRUD with date validation, soft delete and issue detachment; scope/progress derives from persisted issues; daily burn-up snapshots are persisted in `cycle_history` and exposed through `/cycles/:id/history`; creation allocates team-local numbers and globally collision-safe IDs; persisted team settings cover enablement, 1–8 week duration, start weekday, validated IANA timezone, cooldown, 0–15 upcoming cycles, and active-issue automation; reads reconcile expired/current/upcoming statuses, roll unfinished issues forward, mark the current cycle complete on disable, remove future cycles, and target started/completed no-cycle issues at the current timezone calendar day or the correct previous/next cycle during cooldown; capacity uses configured estimate effort points when team estimates are enabled, otherwise persisted issue scope, with team-member fallback when velocity history is absent, and the UI no longer accepts a fabricated manual percentage; tokenized `.ics` calendar subscriptions are team-scoped, encrypted-at-rest, revocable, and exposed through authenticated controls plus a public bearer feed | Broader history integration coverage and production mutation smoke remain | Add broader integration/E2E coverage incrementally | `cycle-capacity.spec.ts`, `cycles-capacity.spec.ts`, `issue-estimates.spec.ts`, `cycle-settings.spec.ts`, `cycles.service.spec.ts`, `cycle-calendar.spec.ts`, `cycle-calendar-service.spec.ts`; migration/schema check; backend/frontend lint/typecheck/build |
| Views and display options                          | Persist filters, sorting, grouping and displayed properties                                                                                   | Saved view API with workspace/team scope, permission checks and UI CRUD; view detail now scopes live queries to its saved team/project target, sends persisted filter conditions to a validated server-side evaluator (option, multi-option, text, number, date, and nested AND/OR groups), avoids truncating the source set before advanced filtering, and the issue filter builder exposes option, text, and date operators from the header entry point and applied-filter bar; persisted layout/grouping/ordering/display settings restore and failed view/issue/project queries show retryable errors instead of an empty fallback; the header picker now consumes authenticated `/issues/facets` counts scoped to the member's accessible workspace/team set and validates member, label, and project references before returning counts | UI-builder coverage for nested groups and broader integration/E2E coverage remain | Extend nested-group UI coverage and integration/E2E coverage | `advanced-issue-filter.spec.ts`, `issue-facets.spec.ts`; project-service 38/38 suites and 114/114 tests; backend/web typecheck, lint, build |
| Notifications and activity                         | Persist actor, target and read state; manage inbox read/archive actions                                                                       | Real notification persistence with actor/issue resolution, read/unread and mark-all APIs; inbox creation now binds actor to the authenticated caller and validates recipient/issue visibility; runtime-created notifications use UUIDv7 IDs; inbox hydration skips only genuinely deleted issues and surfaces other API/DB failures; issue and project subscriptions are persisted per member, scope-checked, exposed through subscribe/unsubscribe APIs, and issue subscriptions are included in notification recipients; persisted per-member channel/category preferences now have authenticated GET/PATCH APIs and a live settings screen                                                                                                                                                        | Actual external desktop/mobile/Slack/email delivery providers and snooze scheduling remain incomplete                                   | Connect delivery workers/providers incrementally; keep preference persistence truthful until delivery contracts exist | Backend/frontend build, inbox UUID/error-path tests, issue/project subscription regression tests, preference API tests, migration check, production settings smoke                                  |
| File attachments                                   | Upload files to a target issue/project and display only completed files                                                                       | Authenticated `POST /circle/api/uploads/presign`, `POST /circle/api/uploads/:id/complete`, target-scoped list, and target-scoped signed-download APIs persist workspace/team/file metadata; issue detail and project activity use the real presign → direct storage PUT → complete flow, list completed files, enforce the 25 MB client/server limit, and render retryable query/upload errors. Completion performs a storage `HEAD` check and missing S3 configuration returns an explicit 503                                                                                                                                                                                                                      | External S3/MinIO credentials and a full browser upload E2E are still required; signed-download behavior is not yet production-verified | Add authenticated storage E2E once storage credentials are configured                                                 | Upload scope/authorization/download tests, web lint/typecheck/build, migration check, backend typecheck/lint, CI/deploy; production read-only smoke                    |
| Integrations, analytics, administration, importers | Feature-specific workflows documented by Linear                                                                                               | Several screens are placeholders or static data                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Not safe to claim parity without product-specific backend contracts                                                                     | Keep visible limitation and implement incrementally                                                                   | Per-feature acceptance tests                                                                                                                                           |

## Current implementation rules

- Production responses must never fall back to mock IDs or mock records.
- A failed create/detail request must show an error; it must not synthesize a local record.
- Template instantiation must be atomic and must remap every cloned issue relationship.
- Unsupported source references must fail before creating records, or the entire transaction must roll back.
- Every feature slice must pass local checks, CI, deployment and post-deploy browser verification.
- Frontend deployment revision is a Turbo global environment input, so a changed web source cannot be hidden by a persisted Next/Turbo build cache.
- Saved-view advanced filters are parsed and evaluated by an allow-listed backend contract; malformed JSON, unknown filter shapes, and unsupported operators fail closed with a client-visible API error.

## Additional P0 fixes in progress (2026-09-15)

- CORS is now centralized to production frontend plus localhost development origins across project, auth, user, and notification services; wildcard `enableCors()` calls were removed.
- Production deployment now rebuilds and restarts the `web` service when frontend files or the deployment script change; the prior backend-only deployment path could leave `pm.capylabs.io` on an older frontend build.
- Document and review responses no longer substitute the seeded `ln` member when an author/creator is missing. Documents restrict creator enrichment to the visible team/workspace. Reviews now carry workspace scope through `Migration20260916100000` and enforce it on list/read/update.
- Issue creation now rejects unknown status/priority IDs, requires assignees to be members of both the issue team and its workspace, and validates cycle and parent-issue team membership before persistence.
- The public seed endpoint now returns not-found in production. My Issues counts and lists use only authenticated-user/server issue data; they no longer fall back to the first mock user or local mock records. Template queries also wait for a verified workspace ID during legacy-route redirects.
- Deployment locking now waits for an active deploy to finish for up to ten minutes so a queued release does not fail immediately on a held remote lock.
- Deployment now reclaims all unused Docker objects before `git fetch` and image builds, because the 36 GB production host had reached 100% usage; the cleanup intentionally preserves named volumes and stateful PostgreSQL/Redis data. Corepack is also relocated to a shared image path instead of the deploy user's home cache.
- Member responses now filter returned team IDs to teams visible to the requester, preventing cross-workspace team metadata from leaking through an otherwise valid member lookup. Project reads now reject legacy/corrupt multi-workspace team links instead of returning a project through a secondary team with foreign workspace metadata. The production Agent route now returns an explicit not-configured error instead of canned workspace data; the UI surfaces that API error.
- Agent example prompts now come from the backend endpoint, and production rejects both examples and chat when the integration is not configured; the web page no longer renders a mock skills/canned-response experience.
- Saved-view create/update now validates referenced projects against the view workspace and selected team, preventing cross-workspace or unrelated-team project references.
- Review detail, diff, and guide views now consume persisted API payloads only; synthetic review/diff fallbacks were removed. Review issue references are validated against the same accessible workspace/team, and review timestamps are returned as real relative times.
- Issue creation no longer falls back to the arbitrary `ENG`/`CORE` team IDs. It now requires an actual route/project-selected team and shows an error before sending an invalid request.
- Backend issue and document-folder creation now reject missing team context instead of selecting the first accessible team or legacy `ENG`/`CORE` IDs; the document dialog scopes folder queries to the current team.
- Team/project/issue headers no longer fabricate a `CORE` team while live data is loading or missing. Team lists use persisted cycle and created/updated timestamps, and onboarding rejects an unusable team key instead of silently substituting `ENG`.
- Initiative reads now discard persisted project links whose primary team is missing or belongs to another workspace, and initiative creation requires explicit workspace context instead of choosing the first accessible workspace.
- Issue-template references now require target-workspace members and labels, and enforce mutually exclusive label groups before a template is persisted or updated.
- Issue templates now expose and apply persisted project, cycle, due-date, full status/priority catalogs, labels, and rich descriptions; stale or cross-team project/cycle references are rejected by the backend instead of falling back to current issue context.
- Issue detail reads now discard stale or cross-team relation rows and cross-team sub-issue links instead of exposing persisted identifiers outside the current team.
- Member profiles now use persisted member and label data without `CORE`, generated join dates, generated avatars, or mock label breakdowns. Comment submission no longer inserts a fabricated optimistic activity; API success supplies the persisted activity and API failure remains visible.
- Google OAuth now requires the configured client ID and a verified Google profile; missing configuration or profile data produces an explicit error instead of mock credentials/profile values.
- Notification preference settings no longer expose hard-coded channel statuses or inert switches; until persisted preferences exist, the route shows an explicit unavailable state while the authenticated inbox read/unread/delete contract remains available.
- Review and saved-view creation no longer silently chooses the first workspace for members who belong to multiple workspaces; an explicit workspace is required in that case. Legacy database defaults that could create `CORE`, `ln`, or `circle-workspace` foreign identities were removed and migrated; document-folder creation now requires a real team ID.
- Project-template instantiation now has dedicated regression coverage for parent-key remapping, issue-relation remapping, invalid relation references, and transaction failure visibility; invalid cloned issue or relation creation propagates the error instead of returning a partial project. The authenticated simulation also verifies a cloned relation against the persisted detail API.
- Cycle progress now records one persisted snapshot per cycle/day, merges legacy persisted burn-up points, and exposes historical snapshots through the authenticated cycle history endpoint; completed-cycle graphs no longer depend on a mutable JSON-only counter.
- Cycle creation now allocates team-local numbers from server state and globally collision-safe IDs, so a stale client number or another team’s legacy numeric cycle ID cannot fail creation or leak across teams.
- Cycle cadence now persists a validated IANA timezone and uses that team calendar day for provisioning, status reconciliation, rollover, disable-completion, and active-issue allocation; the settings dialog exposes the persisted timezone instead of silently assuming server UTC.
- Issue filter label options now come from the authenticated workspace label API; the previous bundled mock label catalog was removed from that production path.
- Issue and project mutation paths now reject unknown status/priority/health values, mismatched status categories, invalid project progress, invalid dates, and reversed project date ranges before persistence; focused property-rule tests cover the contract.
- Saved-view detail now applies persisted team/project scope and advanced filters to live API results, restores persisted issue display settings, and no longer exposes an inert issue-detail subscription or attachment/reaction action.
- The editor disables StarterKit's built-in link/underline extensions when registering the configured versions, removing the duplicate-extension warning observed in the authenticated production issue-detail smoke.
- Issue reactions now have an authenticated removal endpoint; the activity UI uses persisted `userIds` to toggle the current member's reaction and rolls back with an error toast if the mutation fails.
- File uploads now have a persisted, target-scoped presign/complete/list contract plus authenticated signed downloads with an explicit unavailable response when storage is not configured; completion never succeeds without a storage existence check. Issue detail and project activity consume that contract through real browser upload flows and only display completed attachments; storage credentials and a browser upload E2E remain unverified.
- Workspace navigation no longer invents a `circle-workspace` route when the authenticated workspace parameter is absent; the affected sidebar surfaces render no workspace links until a real route context exists.
- Label list and group queries now accept and enforce the active workspace, preventing a member’s other accessible workspaces from appearing in the current workspace’s label pickers.
- Label hooks now wait for the authenticated workspace list to resolve the route slug, preventing legacy or foreign route labels from being queried during hydration.
- Team creation now requires an explicit accessible workspace and rejects unscoped teams; corrupt teams without a workspace are excluded from template team filters rather than being matched through an arbitrary workspace. Workspace access now also includes validated ownership, filters direct team membership through real workspace rows, and no longer fabricates a default workspace when none exists.
- Project- and issue-template team checks now reject teams with a missing workspace association as well as teams from another workspace; a malformed legacy team cannot bypass template isolation through a create, update, or instantiate path.
- Issue, project, initiative, view, team, and member list hooks now resolve the active route slug to a real workspace ID before querying; backend list/detail paths enforce the same workspace boundary. Corrupt cycle/project issue rows are filtered by their owning team, and a project’s primary team is retained when secondary team links are present.
- The new-issue dialog initializes only on a closed-to-open transition, so background query refetches cannot erase typed title/description text or steal focus. The frontend workflow catalog no longer offers the `testing` or `blocked` IDs rejected by the issue API; team settings use the live label count and do not report unsupported retirement as successful.
- Initiative project links now reject and suppress projects whose secondary team links belong to another workspace. Project creation no longer invents a start date when the caller leaves it unset; the persisted date remains optional.
- Unsupported settings sections are removed from production navigation and their direct routes show an explicit unavailable state without inert create/filter controls or synthetic empty records.
- `test-all-apis.sh` now delegates to `scripts/circle-user-simulation.sh`, an authenticated simulation harness that creates and verifies workspace/team, project, templates, labels/groups, initiative, cycle, issues, hierarchy, relation, comment, reaction, property updates, and template cloning. It requires an explicit bearer token and workspace/team context (or an explicit `CREATE_WORKSPACE=1` opt-in); it no longer uses unauthenticated calls or fallback IDs.
- Live issue/project display metadata no longer comes from `mock-data`: canonical workflow status, priority, project-health, member-presence, and cycle formatting modules now live under `apps/web/lib`; legacy fixture modules retain only fixture data and type compatibility.
- Project activity health labels/colors now also come from the canonical `apps/web/lib/project-health` catalog; the live activity route no longer imports runtime metadata from `mock-data`.
- Google authentication no longer accepts unverified profiles or mismatched audiences: ID tokens require a verified profile, access-token flows validate Google token audience and `verified_email`, userinfo must match the token email, and database-sync failures abort login instead of issuing a token for a fallback user. Password sign-up no longer generates a synthetic avatar, and Google member IDs use deterministic collision-safe allocation.
- Inbox rendering accepts a persisted notification with no visible actor and renders an explicit unknown-member state instead of dereferencing `null`. Read/unread mutations now roll back the optimistic state and expose the API error when persistence fails.
- SES invite delivery no longer logs a simulated email or returns success without credentials; missing SES configuration is an explicit unavailable delivery result covered by a service regression test.
- Onboarding now requires the authenticated member to exist in the project database; it no longer creates synthetic `@circle.internal` identities or names when auth context is missing. Invite email construction now requires the real workspace slug/name, inviter name, and configured frontend URL; it no longer falls back to `Circle Workspace`, `Workspace Admin`, or localhost.
- The integrations settings route no longer renders a hard-coded provider catalog or fake enabled cards; it shows an explicit unavailable state until provider connection and permission APIs exist.
- Connected accounts and code-review settings no longer claim providers are connected or render an invented diff; both now show an explicit unavailable state until their persisted provider/repository contracts exist.
- Issue subscriptions are now persisted in `issue_subscriptions`, automatically added for issue creators, assignees and mentioned commenters, included in notification recipient resolution, and exposed through authenticated issue subscription endpoints. The My Issues “Subscribed” tab and issue context menu consume the persisted state instead of treating creator/assignee records as subscriptions.
- Project subscriptions are now persisted in `project_subscriptions`, scoped through the project’s accessible teams, and exposed through authenticated GET/POST/DELETE endpoints. The project health popover now renders the authenticated subscription state and links “New update” to the real project activity route.
- Project membership controls now use the existing authenticated `GET/PUT /projects/:id/members` contract; the picker only offers workspace-visible members and backend tests cover cross-workspace rejection before mutation. Decorative Slack, favorite, “learn more,” and activity controls were removed or changed to real navigation/explicit unavailable states.
- The AI & Agents settings surface no longer renders fake enabled integrations, local-only switches, or empty click handlers; it now uses the same explicit unavailable state as other unconfigured integrations. Initiative activity “See all” now navigates to the persisted activity tab.
- Project resources can now be added and removed through the persisted project update API; team resource shortcuts open the live document creation/list surfaces, and unsupported project-status/member-filter actions are disabled with an explicit explanation instead of silently doing nothing.
- Project member controls no longer fall back to a project payload when the persisted member endpoint fails; the picker reports an unavailable state, and the workspace member directory renders a retryable API error instead of an empty list. Member names, emails, teams, and join dates now render directly from the real member API without fixture-style name splitting, hash-based presentation, or a hard-coded year.
- The members header no longer exposes an inert display control or reports a fabricated zero count when the member request fails; it now removes the unsupported control and marks the count unavailable.
- Production web builds now receive the exact deploy revision as a build input, preventing the persisted Next incremental cache from serving stale frontend bundles after a successful image rebuild.
- Primary Teams, Cycles, Initiatives, and Issues list surfaces now expose retryable API error states instead of rendering empty arrays when their live queries fail.
- Cycle issue and project issue surfaces now expose retryable errors for their issue, cycle/project-detail, view, and project queries instead of silently treating failed requests as empty data; activity feeds use an explicit unknown-member label when actor data is unavailable.
- Issue detail now distinguishes a failed issue/activity request from a genuine not-found response and exposes retryable API error states for both.
- My Issues and project overview/activity now expose retryable API error states for failed live issue, project, or detail queries instead of rendering an empty list, indefinite skeleton, or misleading not-found state.
- Projects grouped by team and team-scoped Views now expose retryable errors when their live team query fails. Workspace-scoped Views resolve the real workspace name/icon from the authenticated workspace API; the prior `LNDev UI` and `Circle Workspace` display fallbacks were removed.
- Issue list, search, cycle, project, My Issues, member-profile, and saved-view creation surfaces no longer merge local Zustand records into live results; filter controls receive the caller's scoped API response for option discovery.
- Issue creation for a project no longer selects the first accessible project team when `teamId` is omitted: the backend uses the accessible primary team or requires an explicit team for ambiguous multi-team projects. Project creation also preserves an unset start date instead of fabricating today.
- Personal Preferences no longer exposes local-only switches for behavior that the server does not persist; the default workspace, sidebar customization, and theme controls remain functional, while unsupported home/editor/desktop/automation settings are explicitly unavailable.
- `scripts/circle-user-simulation.sh` now exercises authenticated project subscriptions, project updates/milestones, cycle progress/history, comment mentions/reactions, and backend rejection of conflicting mutually-exclusive labels in addition to the existing template/issue/relationship flow.
- The authenticated simulation harness now asserts the live detail response fields (`activity`, `subIssueIds`, and relation `identifier`) and covers the persisted issue subscription lifecycle (read, unsubscribe, resubscribe); `bash -n scripts/circle-user-simulation.sh` passes. It still requires an explicit bearer token and workspace/team context for execution.
- Initiative and project updates now expose author-scoped PATCH/DELETE APIs and live edit/delete controls. Backend mutations reject a different author, persist activity, return the authenticated detail response, and recompute the parent initiative/project health from the next latest update after deletion. Focused service tests cover author isolation, editing, and latest-health rollback; production mutation smoke remains intentionally unrun because it would modify real workspace data.
- Runtime-created notification, document, review, view, initiative, member, and generated team IDs no longer use timestamp-based fallback IDs; issue relation and project milestone responses now retain authenticated member scope. Inbox hydration no longer swallows non-deleted issue lookup failures. The local repository scan found no tracked AWS/Google private credentials; `.env` remains ignored and `.env.example` contains placeholders only.

## Verification evidence (2026-09-15)

- Google OAuth login succeeded against `https://pm.capylabs.io`; onboarding created the test workspace and Engineering team, and the authenticated app loaded a seeded welcome issue.
- Production cycle smoke after commit `1a6f925`: creating `Parity smoke cycle` in Engineering succeeded after the prior duplicate-primary-key failure; the deployed UI rendered the persisted planned cycle with `0 scope` and `0% of capacity`.
- The supplied `https://pm.capylabs.io/my-workspace/projects` URL is not the canonical workspace route for that account. After `bf0e774`, the authenticated app redirected it to `/tun-lng/my-issues`; label hooks waited for the verified workspace and emitted no new errors. The two retained console errors were timestamped from the earlier pre-fix deployment.
- Local checks after the access-control and multi-team changes: project-service Jest `19/19` suites and `47/47` tests passed; project-service typecheck/lint passed; web lint and Next production build passed. Local PostgreSQL applied all pending migrations, including removal of legacy foreign-identity defaults, and `migration:check` reports `No changes required, schema is up-to-date` against the refreshed tracked ORM snapshot.
- Latest verification for `bf0e774`: project-service Jest `17/17` suites and `44/44` tests passed; backend typecheck/lint, web lint/build, and migration check passed; production health returned `status: ok`; the production origin received CORS credentials while an evil origin received no allow-origin header. CI: [34982897612](https://github.com/TuanChill/nest-turbo-starter/actions/runs/34982897612). Deploy: [34983094734](https://github.com/TuanChill/nest-turbo-starter/actions/runs/34983094734).
- The strict template-team guard was verified locally and in production after commit `21ad1d4`: project-service Jest `17/17` suites and `44/44` tests passed, typecheck passed, and lint reported 0 warnings/errors. CI: [34984371332](https://github.com/TuanChill/nest-turbo-starter/actions/runs/34984371332). Deploy: [34984616456](https://github.com/TuanChill/nest-turbo-starter/actions/runs/34984616456). Post-deploy smoke again redirected the legacy URL to `/tun-lng/my-issues` with the live workspace, Engineering team, and `ENG2-1` issue visible; no new console errors were emitted after the hydration fix.
- Commit `455c581` passed the final local gate: project-service Jest `19/19` suites and `47/47` tests, backend typecheck/lint, migration check, web lint, and Next production build. CI passed in [34987699682](https://github.com/TuanChill/nest-turbo-starter/actions/runs/34987699682); production deploy passed in [34987936747](https://github.com/TuanChill/nest-turbo-starter/actions/runs/34987936747).
- Post-deploy browser smoke with the authenticated Google account verified the supplied legacy URL redirect to `/tun-lng/my-issues`, live Engineering/ENG2 navigation, `/tun-lng/projects` rendering, and team settings rendering a live `0 labels` count and `13 statuses`; cumulative browser logs emitted no new errors. Production health returned `status: ok`; the approved frontend origin received credentials-enabled CORS, while an evil origin received no `access-control-allow-origin` header.
- Full Linear parity is not yet complete: mock-backed legacy surfaces, placeholder integrations/admin/importers, advanced view operators, notification snooze/external delivery, cycle timezone/calendar/capacity semantics, and broader E2E coverage remain explicitly open.
- The authenticated simulation harness was added and syntax-checked locally, but was not run against production in this turn because no bearer token was supplied to the shell environment; production browser smoke remains read-only.
- Commit `a97a837` passed CI but its first production deploy exposed a second host-capacity issue during the final Docker image copy (`ENOSPC` at 100% disk). Commit `a708cb2` changed the bootstrap and deploy cleanup to `docker system prune -af` without `--volumes`; CI [34992170577](https://github.com/TuanChill/nest-turbo-starter/actions/runs/34992170577) and deploy [34992411834](https://github.com/TuanChill/nest-turbo-starter/actions/runs/34992411834) passed. Post-deploy checks returned API health `status: ok`, allowed-origin credentials-enabled CORS, no allow-origin header for an evil origin, and no new browser console errors. The authenticated Google session (`tuanlt203@gmail.com`) loaded the live workspace, `/tun-lng/projects`, and integrations/settings navigation successfully.
- Commit `876042e` moved live catalog imports out of `mock-data`; its first CI run exposed unused legacy icon imports, fixed in `f3aa0de`. CI [34995695068](https://github.com/TuanChill/nest-turbo-starter/actions/runs/34995695068) and deploy [34995906624](https://github.com/TuanChill/nest-turbo-starter/actions/runs/34995906624) passed. Post-deploy Google-session browser smoke loaded `/tun-lng/projects` and `/tun-lng/team/ENG2/all`, showing the persisted `ENG2-1` issue and live status/priority controls with no new console errors.
- Commit `5e186a4` hardened Google authentication and passed the focused profile tests (`2/2`), full project-service Jest (`21/21` suites, `53/53` tests), typecheck, lint, and migration check. CI [34997495587](https://github.com/TuanChill/nest-turbo-starter/actions/runs/34997495587) and deploy [34997695496](https://github.com/TuanChill/nest-turbo-starter/actions/runs/34997695496) passed. Post-deploy browser smoke with the authenticated Google session loaded `/tun-lng/my-issues`, showed the persisted `ENG2-1` issue and Engineering workspace context, and emitted no new browser console errors.
- Commit `f9e9096` recorded the Google-auth verification evidence and passed CI [34998352859](https://github.com/TuanChill/nest-turbo-starter/actions/runs/34998352859) and deploy [34998564510](https://github.com/TuanChill/nest-turbo-starter/actions/runs/34998564510). Health, credentialed CORS, evil-origin rejection, and the authenticated production browser smoke all passed after deployment.
- Commit `360ca8b` removed the inert notification-preference controls and passed local web lint, typecheck, and production build, CI [34998962507](https://github.com/TuanChill/nest-turbo-starter/actions/runs/34998962507), and deploy [34999177345](https://github.com/TuanChill/nest-turbo-starter/actions/runs/34999177345). The authenticated production route `/tun-lng/settings/notifications` now shows the explicit unavailable state and emitted no browser console errors; API health remained `ok`.
- Commit `4b85fff` moved project-update health metadata out of the legacy fixture module and passed web lint, typecheck, build, CI [35000560360](https://github.com/TuanChill/nest-turbo-starter/actions/runs/35000560360), and deploy [35000769655](https://github.com/TuanChill/nest-turbo-starter/actions/runs/35000769655). Production `/tun-lng/projects` showed the live authenticated workspace with zero persisted projects; a missing-project activity route showed an explicit not-found state rather than synthetic detail.
- Commit `138efc0` hardened inbox null-actor rendering and optimistic read-state rollback. Web lint, typecheck, and build passed; CI [35001966630](https://github.com/TuanChill/nest-turbo-starter/actions/runs/35001966630) and deploy [35002146146](https://github.com/TuanChill/nest-turbo-starter/actions/runs/35002146146) passed. Production `/tun-lng/inbox` rendered the authenticated empty state without a new browser error; health and CORS checks passed again.
- Commit `a811178` removed simulated SES invite success and added a regression test for missing credentials. The full project-service suite passed (`22/22` suites, `54/54` tests), along with typecheck, lint, and migration check; CI [35003533897](https://github.com/TuanChill/nest-turbo-starter/actions/runs/35003533897) and deploy [35003713378](https://github.com/TuanChill/nest-turbo-starter/actions/runs/35003713378) passed. Production health remained `ok`.
- Commit `dcbf1a5` added persisted, validated IANA cycle timezones and switched cadence provisioning, status reconciliation, rollover, disable-completion, and active-issue allocation to the team calendar day. Focused cycle tests (12), the full project-service suite (37 suites, 111 tests), backend/web gates, and the migration/schema checks passed locally; CI [35058095445](https://github.com/TuanChill/nest-turbo-starter/actions/runs/35058095445) and production deploy [35058208183](https://github.com/TuanChill/nest-turbo-starter/actions/runs/35058208183) passed. The authenticated production team-settings dialog rendered the persisted `Cycle timezone: UTC` field with no browser errors; health, credentialed CORS, evil-origin rejection, and unauthenticated cycle-settings checks passed.
- Commit `1ec2d9b` removed the static integrations catalog and fake enabled cards. Web lint, typecheck, and build passed; CI [35004201354](https://github.com/TuanChill/nest-turbo-starter/actions/runs/35004201354) and deploy [35004396714](https://github.com/TuanChill/nest-turbo-starter/actions/runs/35004396714) passed. The authenticated production integrations route now shows the explicit unavailable state without new browser errors.
- Commit `a65f30b` recorded the SES and integrations verification and passed CI [35005248884](https://github.com/TuanChill/nest-turbo-starter/actions/runs/35005248884) and deploy [35005436878](https://github.com/TuanChill/nest-turbo-starter/actions/runs/35005436878). Production health remained `ok`, and the Google-authenticated integrations route rendered the explicit unavailable state. The repository contains an S3 helper, but no application module or API currently exposes a persisted upload contract; that remains an implementation gap.
- The upload contract follow-up adds `file_attachments` migration `Migration20260916170000`, workspace/team authorization tests, S3 existence verification, and removes the remaining inert paperclip buttons from issue sub-issue and project activity composers. Production storage was not mutated because no upload credentials or user-requested file were supplied.
- Commit `27db255` removed local Zustand issue-record merges from all-issues, search, cycle, project, My Issues, and member-profile surfaces; the filter bar now receives each caller's scoped API result. Local regression gates passed: project-service Jest `31/31` suites and `82/82` tests, workspace typecheck, backend lint, migration pending check, web lint/typecheck/build, and `git diff --check`. CI: [35034406614](https://github.com/TuanChill/nest-turbo-starter/actions/runs/35034406614). Deploy: [35034564703](https://github.com/TuanChill/nest-turbo-starter/actions/runs/35034564703).
- Post-deploy production smoke for `27db255`: `https://pm-api.capylabs.io/circle/api/health` returned `status: ok`; `https://pm.capylabs.io` received credentialed CORS while an evil origin received no allow-origin header; unauthenticated project-member access returned HTTP 401. The authenticated Google browser session rendered `/tun-lng/team/ENG2/all` with persisted `ENG2-1`, `/tun-lng/team/ENG2/cycles` with persisted `Parity smoke cycle`, and `/tun-lng/projects` with no new browser errors. The supplied `/my-workspace/projects` URL redirected to the authenticated `/tun-lng/my-issues` route.
- The issue filter header now reads scoped facet counts from the authenticated `/issues/facets` endpoint instead of deriving option counts from the capped client list. The endpoint rejects inaccessible team scope and omits assignee, label, and project references that are not valid in the caller's workspace/team scope; focused facet tests and the full project-service suite passed locally. After commit `9d492b9`, CI [35060726280](https://github.com/TuanChill/nest-turbo-starter/actions/runs/35060726280) and production deploy [35060862026](https://github.com/TuanChill/nest-turbo-starter/actions/runs/35060862026) passed. Production health returned `status: ok`; approved-origin CORS returned credentials, an evil origin returned no allow-origin header, unauthenticated `/issues/facets` returned HTTP 401, and the authenticated Google browser showed the live `Todo 1` facet count with no browser errors.
- Cycle capacity first follows the documented velocity behavior at the issue-count level: the backend uses the three most recent completed cycle scopes and falls back to persisted team-member count when no completed cycle exists; the create dialog removes manual percentage entry. Commit `86a0eb6` passed the focused capacity tests, the full project-service suite (`40/40` suites, `118/118` tests), backend/web typecheck, lint, build, migration/schema checks, CI [35063199392](https://github.com/TuanChill/nest-turbo-starter/actions/runs/35063199392), and production deploy [35063328032](https://github.com/TuanChill/nest-turbo-starter/actions/runs/35063328032). Production health returned `status: ok`; approved-origin credentialed CORS passed, evil-origin CORS returned no allow-origin header, unauthenticated cycles access returned HTTP 401, and the authenticated cycles route rendered the persisted `Parity smoke cycle` at `0% of capacity` with the create dialog showing the real velocity-based explanation and no manual capacity field. Browser console errors were empty.
- Team estimates now have persisted scale configuration (`exponential`, `fibonacci`, `linear`, or `t-shirt`), extended/zero/unestimated-value settings, validated issue create/update/template defaults, activity events, an authenticated team-settings UI, and cycle capacity effort-point calculation; disabled teams retain the issue-count behavior. This is covered by `issue-estimates.spec.ts`, `advanced-issue-filter-estimate.spec.ts`, and estimate-aware cycle capacity integration coverage. Broader cycle-history integration remains an explicit gap.
- Commit `3f33f0f` added the persisted estimate migration (`Migration20260917110000`) and passed the local project-service suite (`42/42` suites, `123/123` tests), backend/web typecheck, lint, build, migration-pending, schema-dump, and `git diff --check` gates. CI [35065859703](https://github.com/TuanChill/nest-turbo-starter/actions/runs/35065859703) and production deploy [35066013668](https://github.com/TuanChill/nest-turbo-starter/actions/runs/35066013668) passed; production reported API health `ok`, credentialed CORS for `https://pm.capylabs.io`, no allow-origin header for an evil origin, and HTTP 401 for unauthenticated team access. In the authenticated Google browser smoke, the real Engineering team settings dialog was temporarily enabled and then restored to `Off`; issue `ENG2-1` rendered the persisted estimate selector with `No estimate`, `1`, `2`, `3`, `5`, and `8`; no issue mutation was made and both smoke tabs reported empty browser-error logs. The migration was applied locally with no pending migrations and an up-to-date schema. Broader cycle-history integration remains an explicit gap.
- Commit `364655f` exposed the numeric estimate filter through the shared issue-filter column contract and passed web typecheck, lint, and production build; CI [35068347576](https://github.com/TuanChill/nest-turbo-starter/actions/runs/35068347576) passed. Production smoke initially found that the deployed team-page bundle still served the pre-change filter list even though the web image rebuilt, so commit `7646b2f` clears the persisted Next incremental cache before every production web build. CI [35069856296](https://github.com/TuanChill/nest-turbo-starter/actions/runs/35069856296) and deploy [35070033821](https://github.com/TuanChill/nest-turbo-starter/actions/runs/35070033821) passed; a fresh authenticated Google browser tab then showed `Estimate` in the live filter menu and its `Single`/`Range` numeric controls, with no issue or filter value mutation. The estimate-filter UI is now production-verified; broader cycle-history integration remains an explicit gap.
- Commit `5e336ef` added the scoped `POST /cycles/:id/start-today` operation, next-cycle enforcement, timezone-midnight date adjustment, current-cycle completion/open-issue rollover, future cadence remapping, confirmation UI, and regression coverage. Local full suite passed (`42/42` suites, `125/125` tests), typecheck/lint/build/migration/schema gates passed; CI [35071702026](https://github.com/TuanChill/nest-turbo-starter/actions/runs/35071702026) and production deploy [35071878886](https://github.com/TuanChill/nest-turbo-starter/actions/runs/35071878886) passed. Authenticated Google browser smoke rendered the real Engineering cycle actions menu and confirmation dialog for `Parity smoke cycle`; Cancel was used and no production cycle or issue data was changed; browser error logs were empty. Broader cycle-history integration remains the explicit cycle gap.
- Commits `b84a0b3` and `76b0702` added the team-scoped encrypted calendar subscription entity and migration, authenticated create/rotate/revoke endpoints, public tokenized `.ics` feed, calendar dialog with copy/open/revoke controls, and regression coverage. Local project-service Jest passed (`44/44` suites, `130/130` tests), backend/web typecheck, lint, and production builds passed, `migration:pending` reported no pending migrations, and `schema:update --dump` reported `Schema is up-to-date`; the repository's separate `migration:check` still reports the pre-existing auth-owned `users` table and float-type baseline drift, which remains outside project-service ownership. CI [35074978961](https://github.com/TuanChill/nest-turbo-starter/actions/runs/35074978961) and production deploy [35075162100](https://github.com/TuanChill/nest-turbo-starter/actions/runs/35075162100) passed. Authenticated Google browser smoke rendered the real Engineering `Calendar` control and subscription dialog with empty browser-error logs; no production subscription was created because that would be a real subscribe/unsubscribe mutation requiring confirmation. Read-only production checks returned API health `ok`, approved-origin credentialed CORS, `404` for an invalid public feed token, and `401` for unauthenticated subscription state. Production create/feed/revoke mutation smoke remains the only calendar verification gap.
- Commit `0631d0d` adds retryable error states to My Issues and project overview/activity for failed live queries. Local project-service Jest passed (`44/44` suites, `130/130` tests), backend typecheck/lint/build, web lint/typecheck/build, migration pending, and schema dump all passed. CI [35077774386](https://github.com/TuanChill/nest-turbo-starter/actions/runs/35077774386) and deploy [35077972408](https://github.com/TuanChill/nest-turbo-starter/actions/runs/35077972408) passed. Post-deploy smoke returned API health `200`, credentialed approved-origin CORS, no allow-origin for an evil origin, unauthenticated projects `401`, invalid calendar token `404`, and frontend auth redirect `307`; the authenticated Google browser rendered My Issues (`1 issue`), Projects (`0` persisted projects), and the Engineering Cycles Calendar dialog with no browser errors. No production mutation was made.
- Commit `9323287` removes fake workspace display fallbacks from Views and the workspace switcher and surfaces team/workspace query failures with retry controls. Local web lint, typecheck, production build, and `git diff --check` passed; CI [35079707170](https://github.com/TuanChill/nest-turbo-starter/actions/runs/35079707170) and backend deploy [35079896845](https://github.com/TuanChill/nest-turbo-starter/actions/runs/35079896845) passed. The first smoke briefly observed the prior Vercel bundle, but after the Vercel auto-deploy completed, a fresh authenticated browser load showed the real `Tuấn Lương's Workspace` in Views and no `LNDev UI` fallback; browser console errors were empty. `pm.capylabs.io` is Vercel-backed and the repository workflow SSH-deploys the Docker stack; the delayed frontend propagation is now verified, with no production mutation made.
- The onboarding/invite hardening passed the local full project-service suite (`45/45` suites, `131/131` tests), backend typecheck/lint/build, root typecheck, web lint/typecheck/build, migration pending, schema dump, and `git diff --check` before push.
- Commit `7918444` removes synthetic onboarding identities and invite-email fallbacks. Local project-service Jest passed (`45/45` suites, `131/131` tests), backend/root typecheck, backend lint/build, web lint/typecheck/build, migration pending, schema dump, and `git diff --check`; CI [35081881293](https://github.com/TuanChill/nest-turbo-starter/actions/runs/35081881293) and backend deploy [35082064786](https://github.com/TuanChill/nest-turbo-starter/actions/runs/35082064786) passed. Production health returned `200`, approved-origin CORS allowed credentials while the evil origin received no allow-origin header, unauthenticated onboarding/projects returned `401`, authenticated My Issues rendered real `ENG2-1`, authenticated Views rendered the real workspace name after Vercel propagation, and browser console errors were empty. No production mutation was made.
- Commit `e30279b` adds an authenticated same-team searchable parent picker to issue-template settings, clears the selection when the team changes, and reports parent-query failures instead of accepting an unscoped identifier blindly. Project- and issue-template list failures now use the shared retryable query-error state. Local web lint/typecheck/build, project-service Jest (`45/45` suites, `131/131` tests), backend lint/build/typecheck, root typecheck, migration pending, schema dump, and `git diff --check` passed. CI [35083334843](https://github.com/TuanChill/nest-turbo-starter/actions/runs/35083334843) passed and backend deploy [35083497616](https://github.com/TuanChill/nest-turbo-starter/actions/runs/35083497616) passed after an 8m07s SSH step. Production API health returned `200`, approved-origin credentialed CORS passed, evil-origin CORS returned no allow-origin header, and unauthenticated issue-template access returned `401`; the authenticated Google browser had no console errors. The frontend smoke still served the previous Vercel bundle (raw parent-ID field), so the new picker is not yet marked production-verified; the milestone picker and broader template E2E coverage remain open.
- The follow-up template batch adds project-scoped live milestone selection, backend rejection of a milestone that is not in the configured project, and project-template milestone remapping to the created milestone name. Local project-service Jest passed (`45/45` suites, `133/133` tests), backend typecheck/lint/build, root typecheck, web lint/typecheck/build, migration pending, schema dump, and `git diff --check` all passed. Commit `3d93a4a` is pushed; [CI run 35085677482](https://github.com/TuanChill/nest-turbo-starter/actions/runs/35085677482) and [backend deploy 35085811347](https://github.com/TuanChill/nest-turbo-starter/actions/runs/35085811347) passed. Production read-only smoke passed: project-service health HTTP 200, approved CORS allowlist, evil-origin response without allow-origin, and unauthenticated template access HTTP 401. The authenticated Google production UI now exposes the live parent picker; the workspace currently has no selectable project, so the project/milestone picker cannot be exercised there without creating production data. Frontend propagation remains externally managed by Vercel and is not represented by this repository's CI/deploy workflow.
- Commit `74761a1` adds author-scoped edit/delete APIs and live controls for project and initiative updates, with latest-health rollback after deleting the latest update. Local project-service Jest passed (`47/47` suites, `139/139` tests), backend/web typecheck, lint, production builds, migration-pending, schema dump, and `git diff --check` all passed. [CI run 35088381682](https://github.com/TuanChill/nest-turbo-starter/actions/runs/35088381682) and [backend deploy 35088551636](https://github.com/TuanChill/nest-turbo-starter/actions/runs/35088551636) passed. Production read-only smoke returned health HTTP 200, approved-origin credentialed CORS, no allow-origin header for an evil origin, and unauthenticated issue-template HTTP 401; authenticated Google routes `/tun-lng/initiatives` and `/tun-lng/projects` loaded with empty browser error logs. Production update mutation smoke remains intentionally unrun because it would modify real workspace data; the workspace currently has no persisted projects to exercise that flow safely. This behavior follows Linear's documented author edit/delete update model and latest-update health controls ([Initiative and Project updates](https://linear.app/docs/initiative-and-project-updates)).
- Commit `eafbff6` removes timestamp-based runtime fallback IDs, makes inbox issue hydration surface real failures, and preserves authenticated scope on issue-relation and project-milestone responses. Local project-service Jest passed (`48/48` suites, `141/141` tests), root typecheck, project-service lint, web lint/build, migration-pending, schema dump, and `git diff --check` passed. [CI run 35090719688](https://github.com/TuanChill/nest-turbo-starter/actions/runs/35090719688) and [backend deploy 35090872735](https://github.com/TuanChill/nest-turbo-starter/actions/runs/35090872735) passed. Production read-only smoke returned health HTTP 200, approved-origin credentialed CORS, no allow-origin header for an evil origin, and unauthenticated inbox/documents-folder access HTTP 401; authenticated Google `/tun-lng/inbox` and `/tun-lng/settings/notifications` loaded with empty browser error logs. No production mutation was made.
