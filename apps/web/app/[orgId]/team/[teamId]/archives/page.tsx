import MainLayout from '@/components/layout/main-layout';
import TeamArchives from '@/components/common/issues/team-archives';

export default function TeamArchivesPage() {
   return (
      <MainLayout>
         <TeamArchives />
      </MainLayout>
   );
}
